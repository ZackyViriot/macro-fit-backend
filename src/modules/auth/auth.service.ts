import { Injectable } from '@nestjs/common';


// This is where all the logic for auth will be and it will get -> sent to the auth controller -> then the auth module -> which will be put inisde the main app module folder.
@Injectable()
export class AuthService {}
